# AGENTS.md — Development rules for Trifold

## General

- **Issue before feature**. Before implementing a major new feature, record it as a GitHub issue in this project (`https://github.com/jaakla/trifold`). The issue body must contain the full plan (design, API shape, affected files, benchmarks intended). Pull requests implementing the feature must reference the issue number.